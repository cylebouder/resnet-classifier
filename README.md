# Multi-Model Image Classifier

Three image classifiers in one app: ResNet-50 (1,000 ImageNet categories), a fine-tuned dog breed classifier (37 breeds), and a fine-tuned food classifier (101 categories). Upload a photo to get top-5 predictions from any model.

Built with PyTorch + Streamlit. Dog and food models trained via transfer learning on ResNet-50.
